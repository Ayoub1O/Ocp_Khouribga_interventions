spring:
  application:
    name: itsm-platform
  datasource:
    url: jdbc:postgresql://localhost:5432/itsm_platform
    username: itsm
    password: itsm
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

